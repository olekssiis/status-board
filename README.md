Backend setup (Uses Node.js and Express)



Run:

cd backend

npm install

node index.js



runs on: http://localhost:5000



Frontend setup(Uses React with Vite and TypeScript)



Run:

cd frontend

npm install

npm run dev



runs on http://localhost:5000





\#As of now, pressing publish does not seem to do much. My next step would be to check my errors to make sure the status gets posted.

